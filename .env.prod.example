
# arachne_fullstack - Production Environment
# Generated by fastapi-fullstack v0.1.10
#
# IMPORTANT: Copy this file to .env.prod before deploying
#   cp .env.prod.example .env.prod
#
# WARNING: Never commit .env.prod to version control!

# === Container Prefix (must be unique per server) ===
CONTAINER_PREFIX=arachne_fullstack

# === PostgreSQL ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=  # Generate: openssl rand -base64 32
POSTGRES_DB=arachne_fullstack

# === Redis ===
REDIS_PASSWORD=  # Generate: openssl rand -base64 32

# === JWT Secret ===
SECRET_KEY=  # Generate: openssl rand -hex 32

# === Internal API Key (Server-to-Server) ===
# Shared secret for Next.js frontend to bypass CSRF protection
# Must be set to the same value in both backend and frontend
INTERNAL_API_KEY=  # Generate: openssl rand -hex 32

# === Flower Monitoring ===
FLOWER_USER=admin
FLOWER_PASSWORD=  # Generate: openssl rand -base64 16

# === MinIO ===
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=
MINIO_SERVER_URL=  # e.g., mydomain.com:9055 or minio